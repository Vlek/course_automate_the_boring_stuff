"""
There are also a number of modules that are available via pypi.

In order to use these, they must first be installed using Python's
module manager pip. (This is similar to Ruby's gem or Node.js's npm)
"""
